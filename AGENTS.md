# 衣装智换插件项目协作规则

本文件承接上级 `../AGENTS.md`。修改前核对当前源码、插件安装位置、登录与任务状态；历史知识只作为线索。

## 产品范围

对外统一入口名称为“米哟无限复刻”，统一登录后默认进入 `/` 三项目选择首页。“衣装智换”保留为其中一个子项目名称；插件 ID `czmiyou-yzzh`、产品 4 / `miyo_fashion` 及既有数据目录保持稳定，显示名称调整不迁移历史任务或账单。

插件中的三个项目必须一起维护和验收：衣装智换、虚拟人物复刻重绘、写实虚拟人像复刻重绘。它们共用登录、平台服务配置、通道、素材、任务恢复和中央记账，各自的分镜、人物绑定及成片策略保留。

普通用户只需安装插件和登录米哟账号。平台模式的供应商 Key、人物库 AK/SK 和存储配置留在服务器；不把共享密钥发给本地插件。按现有画布和创作中心的规则接入中央权限、通道和计费，产品归属为 `miyo_fashion` / 产品 4。

## 每一步的提交与插件同步

用户明确要求：每完成一个修改步骤，验证通过后做一次 Git 提交，方便错误回退，并同步更新插件。

1. 开始前查看 Git 状态，保留用户已有改动。每个步骤保持范围明确，只提交该步骤的源码、测试和必要文档，不把其他人的进行中修改或运行数据一起提交。
2. 运行与本步改动相关的检查；通过后本地 Git 提交，并在交接中记录提交号、验证结果和剩余问题。用户未要求推送时，不把本地提交自动推送到远端。
3. 改动影响客户端时，同步源码、可安装插件包、固定实际运行安装目录及已注册插件缓存。先备份被替换文件，更新对应校验清单并核对内容；新增运行模块必须同步构建白名单和发布校验规则。
4. 区分文件已同步、当前进程已加载、云端已部署和真实工作流已验收。未运行对应验证时不能报告全部可用。
5. 有运行中或结果不明的任务时，不通过清空记录、强制重启或重复生成来应用更新。保留现场并报告需要安全加载更新的时机。回退优先使用明确的 Git 提交；不执行会丢掉用户改动的强制重置。
6. 同步修改涉及的项目 Skill、安装/构建/校验脚本与说明，避免安装包和执行规则不一致。服务器变更单独记录部署需求，不把服务器凭据或数据库配置打入客户端包。

## 验证与数据边界

- Python 项目使用项目或固定安装目录的虚拟环境执行 `python -m unittest`；页面交互使用现有 Node 测试。
- 本地合成测试、服务状态检查、真实供应商生成和真实中央账单是不同层级的证据。
- 不读取或输出真实 Key、密码、连接令牌及 `.env` 内容。用户未授权时不上传素材、不代点生成或付费批准。
- 不删除历史任务、素材、未决网络记录或账单来消除异常。已有任务查询与恢复必须保持账号及原通道归属，结果不明不得自动重新提交。
- 公共人脸/深度模型安装需校验文件完整性并执行本地合成推理；存在模型文件不等于可用。

## 本次步骤确认

- 平台模式三个项目以一次用户点击的创作步骤为批准范围：先展示上传、模型调用与费用，用户确认一次后完成范围内请求；不能按素材重复弹窗，不能把正常发送中误报成未知回执。
- 批准绑定服务端操作 ID、账号、会话、配置和本次输入；新点击、修改配置、取消或结果不明不能沿用旧批准。旧客户端的单次上传确认不升级为步骤批准。人物入库的本次使用权与审核同意必须明确勾选。
- 测试必须覆盖三个项目、多素材与模型调用串联、并发确认、防重复点击、旧轮询回包、取消、账号隔离和未知回执。
