# 衣装智换 / DepthFlow 视频重绘工作台

本仓库是当前衣装智换主项目，远端为 `wangge666666/czmykj-yzzh`。它不是旧 `CZMIYOU_yizhuan` 的 Next.js `stream_run` 文字工作流，而是 Python/Flask 本地视频工作台，覆盖分镜、人脸隐私处理、表演分析、白模绿幕、人物/服装/场景映射和最终视频重绘。

## 接手顺序

1. 先读根目录 `../AGENTS.md`、`../README.md` 和 `../SYSTEM_RELATIONSHIP.md`，确认当前项目映射与跨系统边界。
2. 再读本仓 `README.md`、`docs/FUNCTION_STATUS.md`、`.env.example` 和相关测试。
3. 当前事实以代码、Git 状态、测试和实际运行结果为准；日志、README、测试快照或知识库只作为线索，不能覆盖实况。
4. 工作区全量验证入口为 `bash ../scripts/workspace-validate.sh`；本仓定向验证使用项目测试虚拟环境运行 `python -m unittest discover -s tests -v`。

## 第一原则：不删除数据库数据

- 不删除、清空或覆盖用户素材、`runs/` 任务、中间结果、模型、浏览器配置或历史输出；需要清理时先确认可恢复性和具体目标。
- 不读取、展示、提交或复制 `.env` 中的真实 API Key、AK/SK、Token、签名 URL、客户素材或未脱敏日志。
- `.env.example` 只能保留变量名和无敏感默认值，不能写入真实凭据。

## 生成、计费与恢复边界

- Seedance、Seedream 和云端表演分析属于可能付费的阶段；每个付费阶段都必须保留用户单独确认，不得为了“自动恢复”静默提交。
- 创建付费任务只发送一次。网络中断或响应不确定时先查询或恢复既有任务，不能盲目重试导致重复计费。
- 本仓已实现 CZMIYOU HMAC 登录、产品 ID 4 / `miyo_fashion` 时间卡校验、余额预检、供应商成功后中央 `token_usage` 幂等记账及任务归属隔离；但在真实环境完成账号、数据库、模型价格和付费任务端到端验收前，只能称为“代码已接入”，不能称为“线上已验收”。
- 本地单元测试不等于真实 ARK/Seedance 调用、真实扣费、真人素材授权或成片质量验收。

## 素材与合规边界

- 人脸马赛克只是隐私保护辅助，不代表已经获得肖像、原片或目标人物授权；提交云端前必须人工逐镜复核漏脸。
- 真人目标人物必须遵守供应商可信素材和本人授权要求；不得通过提示词、打码或本地中转规避平台审核。
- 本地参考视频临时公网暴露时，只允许最小范围、短时、可撤销的通道；任务完成后按现有实现清理，不能把素材长期公开。

## 开发与验证

- 使用 Python 虚拟环境安装依赖，不污染系统 Python；不提交 `.venv*`、`runs/`、`models/`、`logs/` 或生成媒体。
- 修改工作流、付费请求或恢复逻辑时，至少覆盖成功、明确失败、提交结果不确定和重启恢复四类路径。
- 修改衣装替换时，优先运行 `tests/test_wardrobe_swap.py`，再运行全量 `unittest`；涉及网页流程时同时覆盖 `tests/test_web_app.py`。
- 任何真实生成验收都要明确记录：使用的功能阶段、任务 ID（脱敏）、结果文件、是否产生费用、是否发生重试，以及人工检查结果。
