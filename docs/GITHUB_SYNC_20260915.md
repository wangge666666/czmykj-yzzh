# 2026-09-15 GitHub 功能同步

## 合并范围

将本地功能提交 `799039f` 与远端 `main` 的 `33770b1` 合并，保留双方历史。包括人物动作迁移、动态人物/物品/场景替换、按秒改写、多人物内嵌选择与红白黄蓝绑定、人物入库和预览、逐人可选服装、短片补黑与长片分段、原片对白传递。远端的平台登录、账号隔离、只读公司角色库、批准与任务恢复继续保留。

同步新增模块的插件构建白名单、独立发布校验清单、使用说明和项目规则。修复 Windows UTF-8 读取、浏览器静态资源字节校验及并发任务记录短暂文件占用的兼容问题。文件发布失败时保留上次完整记录，临时占用只做有限重试。

## 验证

- Python：`python -m unittest discover -s tests -p 'test_*.py'`，634 项，633 项通过、1 项跳过。跳过原因是本机账号没有创建符号链接的权限。
- Node：`tests/*.cjs` 共 15 组全部通过，覆盖人物上传与更换、共享角色权限、预览、多人物、可选服装、改写素材和音频/分段交互。
- 插件包：`packages/czmiyou-yzzh-sync-20260915.zip`，独立校验通过，共 105 个清单文件。安装包仅保存在本地，不提交 ZIP 到源码仓库。
- 包内运行检查：解包后导入应用，衣装智换、人物动作迁移、视频改写页面均返回 200。
- 未将原始素材、生成视频、运行记录、真实配置或密钥纳入 Git 提交。

## 验证边界

上述是本地自动化和打包验证，没有新调用付费生成或真实中央记账。提示词的实际生成质量、口型和模型审核仍需真实生成验证；给旧视频恢复音轨不会使已经闭合的嘴自动张开。

未部署云端服务，未更新其他电脑上的实际插件安装及注册缓存，也未强制重启现有本地视频服务。源码更新、安装包完成和正在运行的进程加载更新是不同状态。
