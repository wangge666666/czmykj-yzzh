# 访谈记录：衣装智换客户版插件

创建日期：2026-09-06

## 用户原始表达

将 czmykj-yzzh 改为 Codex 或 WorkBuddy 中可直接使用的插件；用户已确认面向客户，保留 CZMIYOU 登录、产品 ID 4、时间卡和中央计费。

## 已确认

- 插件项目：衣装智换客户版插件
- 项目 slug：`czmiyou-yzzh`
- 用户原话：“现在我想把这个项目修改为插件形式的，在codex或者workbuddy中可以直接使用的插件”。
- 第一轮问题：第一版给自己用，还是面向客户并接入登录、收费？
- 用户回答：“面向客户，保留 CZMIYOU 登录、时间卡和中央计费”。
- 产品 ID 沿用 4；不改动其他产品的账号、余额或订阅数据。

## 假设

- 先保留现有视频业务能力及原网页，不删除历史仓库，不改变现有部署。
- Codex / WorkBuddy 负责自然语言编排；任务事实、素材版本、执行结果和恢复记录必须持久化，不能只留在聊天里。
- 客户插件不得携带中央 MySQL 密码、登录签名密钥、火山 AK/SK 或平台 API Key。
- 本轮只做只读核验和本地可逆准备；未获真实扣费、素材上传、全局安装或公开部署授权。

## 待追问

- 第二轮已确认：用户原话“按‘本地＋云端’方案推进”。客户电脑执行预处理，中央服务执行鉴权、付费生成和记账。
- 服务器域名、实际账户联调和双宿主安装验收留到相应阶段确认，不妨碍当前代码盘点。

## 追加需求澄清：客户自行提供 API Key

- 用户明确的交付体验：提供一个压缩包给 Codex 或 WorkBuddy，由 Agent 引导安装和使用；不要求客户部署配套后台。
- 用户最新原话：“我只是提供插件包，剩下的需要付费的，比如生产视频啥的，这些让用户自己接自己的apikey就可以了我感觉，类似一个evn文件？”
- 当前理解，待收口确认：转为用户自带供应商 API Key，模型费用由供应商直接向用户收取，不再通过 CZMIYOU 代扣生成用量费用；本地配置文件可使用 `.env`，分发包只包含空值模板，不能包含真实凭据。
- 尚待确认：CZMIYOU 登录和时间卡是否保留，仅用于验证插件购买/使用资格。此项与模型调用费用分开，不因用户自带 Key 自动删除。
- 本轮仅记录范围变化并确认业务边界，尚未改动原需求合同、运行代码、已有账号数据或部署；不读取用户密钥，不发起真实付费调用。

## 最新确认：保留授权，生成改为用户自带 Key

- 对“CZMIYOU 登录和时间卡还保留吗，仅用于验证客户是否有资格使用插件？”的提问，用户回答：“这些需要，应该如何实现那？”
- 当前需求以本节为准：保留 CZMIYOU 登录、产品 ID 4 和时间卡授权；模型生成由客户配置自己的供应商凭据，费用由供应商向客户收取，不走 CZMIYOU 中央余额扣费。
- 原“本地＋云端”调整为本地插件＋既有账号中心授权＋客户自己的供应商 API；不再要求部署独立的视频生成中转服务。
- 本轮请求为实现方式说明。同步更新 01—03 需求和设计；运行代码、已有 ZIP、账号中心、数据库及线上部署均不改动。已有中央代收费预览的测试不能作为新模式的验收。
- 待真实验证而非待业务确认：线上账号接口是否与当前源码一致、产品 4 真实时间卡、供应商直连与双宿主安装。首次版本在线验权、到期后允许取回已有结果等属于设计建议，尚未实测。

## 实施授权

- 用户最新回复：“改造”。按已确认 BYOK 设计实施本地插件、测试与新分发包。
- 不包含真实账号登录、读取用户 Key、供应商消费、私有素材上传、全局宿主安装、账号系统部署或 Git 推送授权。
- 第一版适配火山方舟＋客户自己的北京区 TOS；已向用户说明参考视频上传还需要客户存储凭据，不是只填任意一个模型 Key 就能运行全部功能。

## 原界面修正确认

- 用户质疑：“但是我看着为什么和原来的界面不一样了？”
- 助手澄清：保留原来的界面和操作流程，只增加登录、自己的 Key 设置，以及 Codex / WorkBuddy 操作入口。
- 用户回答：“是的”。按此修正主入口，不把简化工作台当作原产品替代品。
- 0.3.0 恢复原主页、页面资产与业务进程。真实活体 H5 回调未接入，已向用户说明应先使用火山已认证的 Active 资产；此缺口不能冒称验收完成。
