# 开发计划：衣装智换客户版插件

创建日期：2026-09-06

## 0.4.0 自动上传与虚拟人像前台

已完成：主表单只填模型 Key，TOS 为条件必填高级选项，保留完整旧 TOS 配置；统一人物/视频上传适配、严格目的地与逐次确认、上传后读取字节检查。只使用 AIGC 库，原写实流程显示为写实虚拟人像，不提供真人活体授权。原页面业务、路由与产物数据保留。

证据：Python 3.12 auto_upload 10/10、original 13/13、BYOK 21/21、登录 11/11、hybrid 47/47（包含旧中央模式回归，不充当新业务验收）；JS 语法、manifest/skill/69 文件 ZIP 验证通过。真实 Litterbox 合成图片 1026 B 与合成视频 3325 B 上传、读取校验通过，无用户素材与付费生成。隔离包实际 stdio 12 工具、本地 FFmpeg 分镜与导出通过。浏览器验证仅填测试 Key 保存和 TOS 条件展开，真实 17872 已更新 0.4.0，原数据保留，重新登录待用户操作。

## 统一登录产品入口增量

0.5.0 本地实现并完成合成账号浏览器交接：客户登录页保留；产品 4 点击改为本机唤醒/安全会话交接/下载兜底。仅修改相关入口，其他产品既有跳转保持。注册脚本仅显式运行，不执行全局注册或线上部署。Python 新增 7/7、中央入口 TypeScript 3/3，类型检查通过；覆盖错误来源/窗口/state、一次性 nonce、超时、重放、旧账号保护、过期卡和阻止弹窗。Chrome 实际打开原衣装页并显示合成账号/时间卡；发现静态目录被原 UI 覆盖导致 launch.js 404，修复为显式本地文件发送并加入 HTTP 回归。未检测到后的 GitHub 分支也在 Chrome 实际触发。真实系统协议冷启动、线上 HTTPS 入口、真实账户及双宿主仍待相应安装/发布与用户验收；不把测试夹具当作这些环节通过。

## 0.3.1 登录身份修复

已完成：用户确认员工身份并授权修复；两个登录入口增加身份选择，HTTP/运行时/账号适配器透传 role，已知拒绝原因显示中文且不回显上游内部文本。Python 3.12 登录新增 11/11、BYOK 21/21、原流程 13/13、hybrid 47/47；新 ZIP 67 文件校验、独立安装、解压包 11/11 登录回归及实际 stdio/FFmpeg 原流程通过。

真实局部验收：预览更新为 0.3.1 后，用户在页面自行登录；主代理只读确认 logged_in=true、product_4_licensed=true，尚未配置供应商。没有代填密码或调用付费接口；这次正常账号与产品 4 验证不覆盖全部角色、审批异常及叠加卡的线上情况。浏览器旧标签页切号门禁按设计拒绝，刷新后正常进入已登录界面。

## 原界面修正：用户已确认

| 切片 | 状态 | 验证 |
|---|---|---|
| 原主页和全部工作流资产复用、附加账号面板 | 本地验证通过 | 页面除注入脚本外一致，静态资产字节一致；实际浏览器核对原主页、衣装四模式和六步骤及设置面板 |
| 原业务进程账号隔离、会话与网络确认边界 | 本地/模拟验证通过 | original 13/13：未登录/旧会话拒绝、配置/账号隔离、内部凭据、未批准零外发、未明响应阻止重发、netrc 隔离 |
| 原长视频本地拆镜、状态与媒体取回 | 源码和解压包实际通过 | 合成 MP4 经原 multipart API 及 MCP 拆成两段并解码，原下载回退可用，切号不可见 |
| 原 UI 分发包与独立审核 | 0.3.0 技术预览完成 | 67 文件白名单和哈希、隔离 Python 3.12 安装、实际 stdio/原流程、独立 13/13；真实宿主和付费生成仍待授权 |

不能标为完整产品完成：插件内活体 H5 暂不支持；OCR/人声分离及本地模型需另装；供应商、线上时间卡、用户成片和双宿主未验收。最终包路径/哈希见 07。

## BYOK 改造：用户已授权实施

| 任务 | 用户结果 / 文件 | 状态 | 验证方式 |
|---|---|---|---|
| 在线授权与本地配置 | 本地表单登录，产品 4 校验，按账号保存私有 .env；yzzh_local/account.py、settings.py、runtime.py、app.py | 实现及模拟接口测试通过 | `.venv-codex/bin/python -m unittest discover -s tests -p 'test_byok*.py' -v`，21/21：余额零、过期、叠加卡响应、切号、密钥不外显 |
| 供应商直连与恢复 | 本地方案/批准/上传/提交/查询/下载，不走中央计费；provider.py、runtime.py、web | 实现及本地闭环通过，真实供应商待授权 | 同上：无审批/旧审批拒绝、超时不重提、到期取回、合成 MP4 解码；模拟供应商，无费用 |
| 双宿主分发与安装 | 新版本 ZIP、空配置模板、更新技能和 README | 包内独立安装及 stdio 通过，宿主发现待授权 | package 5/5、plugin/skill/release 验证器、setup --dry-run、隔离 Python 3.12 安装，`verify_hybrid_runtime.py --bundle <解压路径>` 的实际导入/分镜/导出 |
| 独立审核与返工 | 最小权限、归属和费用异常边界的独立只读检查 | P2 已修复，主链路与分发增量独立复查通过 | 独立 reviewer 最终 BYOK 21/21、包 5/5；28 个解压载荷与源码/清单逐项一致；无剩余已证实 P0—P2，记录 06-review.md |
| 线上及用户验收 | 真实登录/时间卡、自有供应商生成、客户视频、宿主发现 | 等待相应授权 | 指定账号与费用上限后实际操作；不得把模拟测试记为通过 |

旧运行时另存 yzzh_cloud/legacy_client.py，仅供历史网关回归，不进入客户 ZIP。原网页与中央服务源码保留，不改线上服务。本轮不安装到全局宿主、不推送、不读取真实密钥或调用付费服务。

## 历史中央代收费预览

> 范围变更：用户已确认“保留 CZMIYOU 产品 4 时间卡授权＋客户自带供应商 Key”。当前目标见 01—03；下表及测试数字仅为旧中央代收费预览的历史进度，不能代表新模式完成。新的运行代码改造计划尚待进入实施阶段，本轮未修改代码或部署。

| 任务 | 状态 | 用户结果 / 文件 | 验证 |
|---|---|---|---|
| 访谈及混合形态 | 已确认 | 本地预处理＋中央商业服务，00—03 文档 | 用户本轮回答 |
| 本地处理与持久恢复 | 代码完成，部分运行通过 | yzzh_local；导入、分镜、打码、深度、结果预览 | 合成 MP4 实际分镜解码及重启恢复通过；打码/深度未安装模型，真实推理待验收 |
| 商业网关与本地调用 | 代码及模拟联调通过 | yzzh_cloud；鉴权、报价、幂等提交、查询、记账、下载 | 真实回环 HTTP/multipart＋模拟账户/供应商；并发、越权、过期报价、账单异常回归通过 |
| 安全工作台与 MCP | 本地运行通过 | 人工审批与 10 个 MCP 工具 | stdio initialize/list/call 和导出通过；浏览器实际预览 4 秒分镜，跨站与越权测试通过 |
| 独立审核和返工 | 实现范围复查通过 | 06-review.md，独立只读 reviewer | 4 项初审问题、导入竞态、排队提交归属均修复并回归 |
| 双宿主分发包 | 技术预览包完成 | 客户白名单包、独立云端部署说明 | 清单、技能、文件哈希、隔离 Python 3.12 安装和解压包 stdio/视频通过；不等于宿主导入通过 |
| 全局安装和真实宿主 | 等待授权 | Codex / WorkBuddy 分别发现与触发 | 用户确认安装后新任务实际调用 |
| 真实账号、付费与素材 | 等待环境及授权 | 产品 4 登录/时间卡/账单、用户视频 | 配置云域名后确认测试账户与费用，生成 MP4 解码并人工检查 |
| 用户验收 | 等待真实产物 | 08-user-check.md | 用户检查，不代填批准 |

本轮 focused 验证命令：`.venv-codex/bin/python -m unittest discover -s tests -p 'test_hybrid*.py' -v`，46/46 通过。全量 297 项中 291 通过，6 项为原版基线失败，详见交付证据。
