# 审核：衣装智换客户版插件

创建日期：2026-09-06

## 0.5.0 主代理定向检查（无独立复查）

新增 7 项本地交接与 3 项中央前端测试通过。核对精确 origin/source/state、一次性限时 nonce、错误 token 不覆盖当前账号、无时间卡拒绝新任务、无浏览器凭据 URL、无下载站 token、未批准不注册系统、未完整安装不写系统协议。实际 Chrome 合成账号跳转原 UI 与下载兜底通过。修复 static_folder 覆盖导致的启动脚本 404，并验证实际 HTTP 返回 JS 字节。未执行真实系统协议注册/冷启动、线上 HTTPS、真实账号或真实宿主验收；本次没有独立 reviewer，不运行严格交付完成检查。

## 0.3.1 登录修复的定向检查

本次为主代理检查，未新增独立 reviewer 复查，不沿用下方旧版本的独立 PASS 作为这次审核结论。已运行新增登录 11/11、BYOK 21/21、原流程 13/13、hybrid 47/47、解压包登录 11/11 和实际 stdio/原流程视频；检查非法身份零网络请求、员工无时间卡仍拒绝、失败登录清旧会话、审批版本失效、200/401/403 拒绝分支、不透传未知上游文本、不自动换身份重试。浏览器与只读状态观察到用户自行完成真实账号登录及产品 4 有效；付费流程和全角色线上边界未验收。

## 0.3.0 原界面独立复查

独立只读 reviewer 报告：本次范围没有剩余已证实 P0—P2，最新 original 定向 13/13 通过。已报并修复：异常成功响应错误解除防重（含人物库）、原下载查询参数、到期恢复被拦、远端人物图签名/CSP、环境 netrc 覆盖账号凭据。

reviewer 另以真实 tos 2.9.2 SDK 和模拟底层传输/审批验证正文冻结、CRC 和单次 PUT；没有外网传输、真实凭据或费用。源码/本地范围复查通过不代表供应商和宿主商用验收。H5 回调缺口已明确，不是通过项。

## 当前 BYOK 0.2.0 独立复查

独立只读审查 account/settings/provider/runtime/HTTP/MCP/UI，发现 1 项 P2：旧 A 标签页可将 A 的 Key 保存到新登录 B，或误清 B 的配置。已修复：设置写入/清除强制绑定 expected_owner 和非秘密 expected_session，锁内核验；界面切会话清空 Key 输入。

独立复查通过：19/19 BYOK 测试；另外双 Flask 客户端模拟旧保存、旧清除、缺绑定和同账号旧会话，均为 409，B 的假 Key 不变。原账号到期恢复、零余额产品 4、无新增付费 POST、输出不回显凭据的 fixture 检查通过。后续补充供应商适配器合约测试，主代理 BYOK 21/21 通过；分发增量复查单独登记。

仅为本地技术预览复查；没有登录真实账号、访问真实供应商或执行付费操作，不代表正式商业验收。

最终分发增量独立复查通过：BYOK 21/21、包 5/5；隔离安装目录 28 个载荷＋1 个清单完整，载荷与当前源码逐一相同。仅空 .env.example 入包，默认无需中央视频网关；MCP 无登录/Key/批准入口；文档与代码一致。独立 reviewer 报告当前无剩余已证实 P0—P2。

## 以下为旧中央代收费预览审核历史

## Findings

按插件构建流程委派独立只读审核，未注册全局 reviewer 或修改宿主配置。

| 问题 | 等级 | 返工与证据 |
|---|---|---|
| 报价期间切换账号，原素材可能被分配给新账号 | P1 | login/logout/plan 共用 RLock；并发阻塞报价回归验证 owner 仍为原账号 |
| 待对账项目允许重新计划或处理，丢失旧任务恢复依据 | P1 | reconciliation_required 加入 BUSY；保留任务与旧计划历史，重启回归通过 |
| 审批页仅有哈希，不能识别实际参考素材 | P1 | 本地图片不可变快照、video/image references 和逐项媒体预览；提交前再次核验哈希 |
| 明确过期 409 被当作不明提交，随后 quoted 被当作 running | P2 | 明确拒绝进入 needs_replan；未知提交保持 submit_uncertain；真实 HTTP 过期报价回归通过 |
| 导入复制期间切账号导致新账号取得视频 | P1，复查新增 | import_video 纳入同一锁，阻塞 copyfile 并发切换回归通过 |
| 排队提交与登录之间的窗口 | 主动加固 | worker 在打开文件前重查归属；账号变化时零上传请求 |

## Recheck

独立审核者复查原 4 项与新增导入问题，重跑 43 项时全部通过，报告该实现范围没有剩余 P0—P2 阻断。随后追加安装兼容和 ZIP 验证，主代理 focused 总数为 46。

分发增量的两项 P2 已返工：验证器必须固定完整 24 个文件（不能只信任可编辑清单）；Windows 相对路径必须统一为正斜杠。独立重跑打包 4/4 通过，包括删除/增加文件并同步修改清单、普通篡改和 Windows/POSIX 路径一致性，报告分发增量无剩余阻断。

RECHECK_RESULT: PASS

技术预览通过不等于正式客户上线通过。真实宿主、真实账号/时间卡/余额、供应商生成、费用账单和用户视频验收均未完成；云网关单主机 SQLite 和原生市场端点配置的限制在服务说明中保留。
