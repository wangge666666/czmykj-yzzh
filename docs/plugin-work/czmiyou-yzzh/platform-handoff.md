# 三项目平台接入：实现与部署交接

2026-09-07。平台客户端最后业务提交 `c0e01cf`，创作中心服务器提交 `9efc5e1`。改动已本地提交，未推送、未部署、未执行真实数据库写入或付费任务。

## 用户效果

衣装智换、虚拟人物复刻重绘、写实虚拟人像复刻重绘共用米哟登录、平台模型、人物库、素材服务与产品 4 中央记账。普通用户的账号面板显示服务状态与余额，不要求供应商 Key、AK/SK 或桶配置。原有分镜、人物绑定、白模和最终重绘策略保留；本地打码与深度模型仍由完整安装和基础自检保证。

原图和分析视频先通过已批准的平台素材上传取得地址，再交模型。图片保存在画布角色服务的存储中，视频直接使用创作中心存储，避免视频转 base64 后超过画布 JSON 限制。生成输出先存入平台，再记账；取回时按原存储来源重新签名，不使用过期临时链接兜底。

每个云端写请求绑定账号、项目、步骤、精确参数与稳定请求编号。服务器先保存请求再提交，重复请求查原记录。“查询原请求”仅核对回执；缺失回执不当作未受理，已确认任务不重新生成。三项目平台任务与旧 BYOK 历史按账号及模式隔离，保留旧记录。

## 本次验证

- 客户端：122 项 Python 回归（包括真实隔离子进程、合成视频的两类长视频拆镜），35 项页面交互测试。
- 服务器：106 项测试；全仓 TypeScript 检查及新增模块 ESLint 通过。
- 跨端：从原生产函数捕获的 15 组视频、图片、分析、AIGC 参数通过实际服务器校验器；大于 256 KiB 的合成素材通过平台上传转换后，模型请求不含内嵌媒体。
- 插件：明确白名单的 76 个文件通过归档校验。每一步已备份并同步实际安装目录和已注册缓存，未强停运行进程。
- 凭据模式扫描未发现匹配；客户端包不含服务器、数据库、运行数据或真实配置。

这些验证没有使用用户素材、真实供应商或真实中央账单，不能替代生产验收。

## 当前运行现场

只读 MCP health 显示：当前运行版本 0.5.0、仍为 `byok`、登录状态为已登录。此状态来自旧运行进程，不表示新平台已启动。

对当前创作中心 `https://7dqznbpct4.coze.site/api/yzzh/capabilities` 的不带凭据检查返回 404，新服务器接口尚未部署。未通过读取真实令牌来做账号请求，也未为加载更新清除旧任务或网络记录。

## 上线顺序

1. 将创作中心提交 `9efc5e1` 发布到实际创作中心部署。先检查远端分支和已有改动；当前本地提交未自动推送。
2. 管理员在服务器沿用现有 `MIYO_MYSQL_*`、账号签名配置、`MIYO_CREATION_VOLC_API_KEY` / `MIYO_CREATION_VOLC_API_KEY_GUANGDONG`、`MIYO_CHARACTER_ASSET_SERVICE_URL` 与 `COZE_BUCKET_ENDPOINT_URL` / `COZE_BUCKET_NAME`。只核对配置是否存在，不回传真实值。
3. 数据库仅新增 `yzzh_platform_operations`、`yzzh_platform_resources` 两张表；保留所有旧任务、账单与素材。确认产品 4 / `miyo_fashion` 生效，以及两条通道、白模/最终视频、图片、分析的中央价格配置。
4. 发布后匿名能力请求应由新接口返回鉴权错误，而非 404；账号从统一登录进入后，再检查三项目的公开服务状态。状态就绪仅证明配置满足检查。
5. 处理当前 BYOK 未决任务并安排安全退出，重新从统一入口启动默认平台模式。不要强杀正在运行的任务、清空不明记录或把旧任务改用平台重发。再次验证实际 `health.mode=platform`。
6. 用户允许上传及付费后，分别验收三个项目的素材、人物审核、白模、分析、最终成片、刷新/重启恢复与中央账单；核对成功视频文件能解码、费用归属产品 4 且不重复记账。

## 回退与同步规则

`AGENTS.md` 已明确：每个修改步骤验证后单独提交，再同步插件源码、安装包、固定安装目录与注册缓存；备份原文件并更新校验清单。创作中心也写入相同联动规则。

本轮客户端依次提交：`d9d86a4`、`a63a1c4`、`742095f`、`990c180`、`a2b660b`、`dd1f641`、`ed704bd`、`36c793b`、`c0e01cf`。回退选择对应提交，不用强制重置丢掉用户改动；服务器回退代码时保留新增表及其任务记录。
