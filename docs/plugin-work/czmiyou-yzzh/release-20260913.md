# 米哟无限复刻 0.5.0 · 平台模式开发预览

发布标签：`v0.5.0-preview.20260913`。运行时与插件标识版本仍为 `0.5.0`，日期标签区分本次完整分发包。首次发布时仓库为私有；2026-09-13 经用户明确授权，仓库及 Release 附件已改为公开，下载无需 GitHub 账号或仓库权限。平台功能仍需 CZMIYOU 登录和有效产品授权，公开下载不代表免费生成。本次未修改线上登录入口或服务器配置。

## 客户下载

附件 `czmiyou-yzzh-0.5.0-platform-preview-20260913.zip` 是完整客户端包，含 77 个载荷文件和 SHA256 清单。GitHub 自动生成的 **Source code ZIP 不是客户安装包**。模型和 Python 依赖在首装时下载，不随源码 ZIP 分发。

ZIP SHA256：`baf9aa14de987405523b1752e9cce44334d066359f05df719fcc796bc8cb3a93`。

解压到固定目录后，让 Codex / WorkBuddy 阅读包内 README，校验文件并引导安装。需要 Python 3.10+，推荐 3.12；安装独立环境及约 50 MB 的基础人脸、深度模型，不强制安装 Torch、CUDA 或 Demucs。网页自动唤醒另需用户明确同意注册本机启动器，不覆盖已有处理程序。

正常入口：CZMIYOU 统一登录 → 我的应用 → 米哟无限复刻 → 选择衣装智换、虚拟人物或写实虚拟人像项目。新启动的后台默认平台模式，用户不填写供应商 Key、AK/SK 或 TOS。管理员在服务器维护通道、素材库、存储与中央计费；产品 4 时间卡和每步消费确认分别执行。原页面素材、人物绑定、分镜与成片策略保留。

## 本次包含

- 平台模式三项目工作台、统一登录、本地启动器、Codex / WorkBuddy 入口文件与衣装智换 Skill。
- 公司共享角色及本账号角色读取、既有角色 ID 复用、跨镜人物映射的本地核对恢复。
- 本次步骤的上传与费用确认，以及结果不明时查询原请求、不自动重复生成。
- 白模人物统一穿完整不透明白色长袖长裤工作服、手套与平底鞋；不承诺提示文案修改即可通过供应商审核。
- 基础模型文件校验、实际加载与合成推理、三个页面及 FFmpeg 安装自检。

## 发布检查（2026-09-13，macOS Apple Silicon / Python 3.12.14）

| 检查 | 本次证据 |
| --- | --- |
| Python 回归 | 480/480 通过 |
| 前端 Node 回归 | 44/44 通过 |
| 包结构、文件校验和 | 安装前验证 77 个载荷；全部与当前源码逐项一致 |
| 插件清单、单技能清单、技能格式 | 检查通过；项目检查仅普通模式，不声明严格交付完成 |
| 独立目录实装 | 新虚拟环境安装成功；两项基础模型通过下载、哈希校验、实际加载和合成帧推理；三项目文件与 FFmpeg 自检通过 |
| 解压包真实本地处理 | stdio MCP → HTTP 后台 → FFmpeg；4 秒合成视频导出并解码，原虚拟人物流程拆成 2 镜且可解码；平台模式，模拟账号/平台，云端修改请求 0 次 |
| 当前 Codex | 插件 installed/enabled；当前任务可调用 health，已登录、platform；未重新安装或重启用户服务 |
| 本地同步 | 客户包与实际安装、Codex 缓存的 75 项公共载荷一致；宿主特定 MCP 配置和说明另行保留，不覆盖 |
| 敏感信息检查 | Gitleaks 8.30.1 对解压载荷和本次差异扫描无命中；只包含空值 `.env.example`，不含真实配置、用户数据、运行库或模型；扫描不等于绝对安全保证 |
| 公开前历史检查 | 抓取远端分支与标签后执行 Gitleaks `git --log-opts='--all'`；扫描器报告 28 个提交、3 个命中，逐项核实均为测试夹具，仅出现在测试文件中，未发现真实凭据；当时 Actions 运行记录为 0 |
| 公开下载验证 | GitHub 返回 PUBLIC；不带登录凭据访问仓库与 Release 页面均为 HTTP 200；匿名下载完整 ZIP 为 510537 字节，SHA256 与发布资产一致 |

复现包内运行验证：`python scripts/verify_hybrid_runtime.py --bundle <已安装的解压包> --mode platform --data-dir <全新测试目录> --evidence-dir <全新证据目录>`。只能使用新建合成测试目录；不会读取用户现有连接凭据。该脚本的账号与平台均为回环模拟，不调用真实模型或中央账单。

测试校准说明：远端 `2f60260` 有 7 项既有失败，运行载荷提交 `7157138` 另有 1 项旧白模文案断言。`ec00542` 仅校准测试的 macOS 路径、当前文案/请求形式与授权前置夹具；未削弱生产授权，并补上未同意时零上传准备调用的断言。本次运行载荷与 `7157138` 一致，后续仅测试、验收脚本和仓库说明变化。

## 预览边界

这不是全平台正式可用或付费验收完成声明。本次没有用客户素材执行上传、人物审核或付费生成，没有核对真实中央账单；用户此前报告测试正常，与本次自动化检查分别记录。当前登录状态不证明所有账号、时间卡异常分支都已在线验收。

WorkBuddy 提供配置与技能文件，但本次未完成真实 WorkBuddy 宿主安装；Windows/Linux 也未做机器实测。现有 Codex 工具发现已验证，不等同另一个宿主通过。旧 BYOK 仅显式兼容，已有后台不自动切模式；不删除或迁移旧任务。

发布成功以 GitHub 标签、附件存在及下载后 SHA256 一致为准。当前仓库和附件均可公开访问；客户应下载上述完整插件 ZIP，而非自动生成的源码归档。不把服务器密钥或客户数据加入发布资产。公开操作只更新仓库可见性和分发说明，未重建安装包、移动既有标签或改变开发预览状态。
