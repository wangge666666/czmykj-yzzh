# Empty customer template only. Do not put real credentials in the plugin ZIP.
# After login, copy to the per-account config_path shown by the local workbench,
# or fill the local Settings form. No shell expansion; double quotes use JSON.
# Ark generation; temporary upload is automatic after human approval.
# MEDIA_UPLOAD_MODE: temporary or tos. Empty preserves existing TOS configurations.
ARK_API_KEY=
ARK_MODEL=
MEDIA_UPLOAD_MODE=
TOS_ACCESS_KEY=
TOS_SECRET_KEY=
TOS_BUCKET=
ARK_IMAGE_MODEL=
ARK_PERFORMANCE_MODEL=
